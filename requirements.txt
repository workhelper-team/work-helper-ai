# --- Base Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.1
httpx>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.1

# --- LangChain & Orchestration ---
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langgraph>=0.1.0

# --- LLM Providers ---
langchain-ollama>=0.1.0
langchain-openai>=0.1.0

# --- Vector DB & PostgreSQL ---
langchain-postgres>=0.0.6
psycopg[binary]>=3.1.18
pgvector>=0.2.5

# --- Embeddings (CPU-friendly) ---
sentence-transformers>=3.0.0
huggingface-hub>=0.23.0

# --- OCR & Document Processing ---
pytesseract>=0.3.10
pillow>=10.0.0
pymupdf>=1.24.0

# PaddleOCR 기반 (v3.x 호환 버전 고정)
paddlepaddle>=2.6.0
paddleocr==3.7.0

# [참고: GPU(CUDA) 가속 환경 구축 시]
# 위의 'paddlepaddle' 대신 본인의 CUDA 버전에 맞는 GPU 패키지를 설치합니다:
# pip install paddlepaddle-gpu
# (예: CUDA 11.8 기준 -> pip install paddlepaddle-gpu==2.6.2.post118 -f https://www.paddlepaddle.org.cn/whl/linux/mkl/avx/stable.html)